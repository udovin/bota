[package]
name = "bota-bot"
version.workspace = true
edition.workspace = true

[dependencies]
bota-proto = { workspace = true }
candle-core = { version = "0.11", default-features = false }
clap = { version = "4.6.6", features = ["derive"] }
